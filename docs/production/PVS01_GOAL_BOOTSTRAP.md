# WRITEOVER-07 PVS-01

## Grand Objective

Deliver the premium vertical slice path:

`B1 Wake/Calibration -> Logistics -> Service/Medical -> 1F Lobby -> Security Checkpoint -> Restroom/Staff routes -> Elevator Lobby`

at `READY_FOR_HUMAN_PLAYTEST` quality.

## Before work

Read, in this order:

1. `docs/product/ACTIVE_PRODUCT_SOURCES.md`
2. `docs/production/PVS01_EXECUTION_CONTRACT.md`
3. `docs/production/PVS01_GOAL_LEDGER.md`

The contract is the stable protocol. The ledger is the current phase, evidence,
decisions, benchmarks, open issues, deferred work and next action. Do not paste the
old long Goal back into the session.

## Execution protocol

`IMPLEMENT -> TEST -> CODE RED TEAM -> FIX -> REAL PLAY / VISUAL EFFECT RED TEAM -> FIX -> PERFECTION RED TEAM -> POLISH -> LOCAL SHIP -> COMMIT -> CONTINUE`

Keep quality above quantity. Work autonomously and do not wait for routine user
approval. Use official web research when local evidence is insufficient, summarize
the result in the ledger, and never dump whole pages or logs into the Goal.

If blocked: inspect -> reproduce -> try a minimal fix -> consult repository and
official sources -> try materially different alternatives -> controlled defer with
an exact revisit condition -> continue independent work -> revisit before final red
teams.

Preserve the current Git tree, dirty work, assets and history. Do not reset the game,
rewrite unrelated systems, expose secrets, or make paid/irreversible decisions.
After context compaction, reread this bootstrap's two referenced documents and the
ledger before continuing. Keep successful output concise and save visual evidence as
files.

Continue until the Grand Objective is achieved at a documented Victory Ladder level
or a genuine global hard blocker prevents safe progress. Then run the final three red
teams, record the result in the ledger, and stop.
