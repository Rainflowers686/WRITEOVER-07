#include "writeover/common/result.h"

// Implementation intentionally empty: Result is header-only. This TU exists
// so the module has a translation unit for tooling parity. (result.cpp)
namespace writeover {
// (no symbols needed)
} // namespace writeover