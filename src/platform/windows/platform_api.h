#pragma once
// Compatibility include for older platform-only code.  The public platform
// declarations live in the portable include tree so the app has no
// Windows-only header dependency.

#include "writeover/platform/platform_api.h"
