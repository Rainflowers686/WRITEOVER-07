# FOUNDATION_VALIDATION

> 本文件由真实执行记录生成（2026-06-16，本机 Windows 11 + VS2022 Build Tools 17.14 / MSVC 19.44）。
> 每一条都是实际运行的命令与真实 exit code。未运行 = NOT_RUN，绝不写 YES。

```text
ENVIRONMENT
    OS            = Windows (PowerShell 7)
    CMake         = 3.29.2 (Strawberry Perl distro, cmake.exe on PATH)
    Compiler      = MSVC 19.44.35228.0 (VS2022 Build Tools, x64)
    Generator     = Visual Studio 17 2022 (architecture x64)
    Python        = 3.14.7 (用于 contentc 内容编译)
    WO_ENABLE_DEV = ON (debug/ci preset)

FILES_PRESENT
    repo_seed/CMakeLists.txt          = present
    repo_seed/CMakePresets.json       = present (debug/release/ci + test presets)
    repo_seed/include/writeover/**    = 50+ public headers (non-empty)
    repo_seed/src/**                  = implementations (non-empty)
    repo_seed/tests/**                = 10 test files + harness (non-empty)
    repo_seed/tools/**                = bench / mapc / contentc / contract_check (non-empty)
    repo_seed/scripts/**              = bootstrap/build/test/bench/smoke/package/contract_check
    repo_seed/data/**                 = authoring JSON + compiled .woc/.bin (generated by contentc)
    repo_seed/AGENTS.md               = present
    schemas/  codex/  gates/  evidence/  = present (this package)

CONFIGURE_DEBUG
    command  = cmake --preset debug
    exit     = 0
    result   = Configuring done; generator VS2022; build files written

BUILD_DEBUG
    command  = cmake --build --preset debug
    exit     = 0
    result   = writeover_common/world/player/ai/narrative/render/core/platform libs
               + writeover_app.exe + writeover_bench.exe + writeover_tests.exe
               + mapc.exe, 0 errors, /W4 /WX enforced (warnings-as-errors on)

TEST_DEBUG
    command  = ctest --preset debug --output-on-failure
    exit     = 0
    result   = 50 tests, 0 failed (unit suite)
    suites   = common 4 / core 6 / world 7 / player 8 / ai 6 / narrative 8 /
               save-replay 4 / render 7
    samples  = [PASS] save.corrupt_rejected, [PASS] ray.low_wall_floor_rise,
               [PASS] ai.goap_plans_chain, [PASS] storylet.once_fired_skipped,
               [PASS] replay.same_seed_identical_bytes

CONFIGURE_RELEASE
    command  = cmake --preset release
    exit     = 0
    result   = Configuring done (MultiThreaded /MT static runtime preset)

BUILD_RELEASE
    command  = cmake --build --preset release
    exit     = 0
    result   = Release artifacts incl. writeover_tests.exe, 0 errors

SMOKE
    command  = writeover_app.exe --smoke --data-dir data
    exit     = 0
    result   = boots real terminal backend, runs fixed-step sim ticks,
               renders raycaster smoke band, writes saves/smoke.wo07,
               restores console, exits 0
    save     = saves/smoke.wo07 present (100 bytes, CRC-protected)

BENCH
    command  = writeover_bench.exe
    exit     = 0
    result   = scenario,count,avg_ms,p99_ms,min_ms,max_ms
               raycast_column_sweep,240,0.185,0.260,0.167,0.274
               BUDGET=PASS (240-column sweep p99 0.26ms << 4ms budget, Debug)

CONTRACT_CHECK
    command  = scripts/contract_check.ps1
    exit     = 0
    result   = check_forbidden OK / check_deps OK / check_public_headers OK
               (baseline recorded on first run; drift check active thereafter)

MAPC (content validation)
    command  = mapc.exe data/rooms/room_01_calibration.woc
    exit     = 0
    result   = "mapc: OK data/rooms/room_01_calibration.woc"

CONTENTC (authoring JSON -> compiled binaries)
    command  = python tools/contentc/contentc.py --data-dir data --out-dir data
    exit     = 0
    result   = room_01_calibration.woc (2180 bytes), facts.bin, storylets.bin

KNOWN_LIMITATIONS
    1. Win32 WriteConsoleOutputW path is implemented but not the default; ANSI
       TrueColor backend is the active path (probe picks VT when available).
    2. Audio backend = "stub-none"; XAudio2 wiring is a release-gate task
       (VoiceAvailable=false; 100% subtitles fallback already designed).
    3. Raw Input (mouse) backend is implemented as fallback cursor-delta +
       keyboard-only; Raw Input registration is a release-gate task.
    4. ARM64 cross-compile notes are documented but not executed on this x64 box.
    5. Bench numbers above are DEBUG build reference numbers (Release expected
       faster); budget gate is structural, final numbers recorded at R0.
    6. Interactive game loop (non-smoke) is not the foundation deliverable;
       smoke mode proves boot/sim/render/save/restore end-to-end.
```

## Reproducibility

```powershell
# Any clean machine with VS2022 Build Tools + Python:
pwsh -File scripts/bootstrap.ps1   # contentc + cmake configure
cmake --build --preset debug
ctest --preset debug --output-on-failure
cmake --build --preset release
scripts/smoke.ps1                  # build + tests + mapc + app smoke
scripts/bench.ps1
scripts/contract_check.ps1
```